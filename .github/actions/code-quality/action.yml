name: Code quality
description: Run Biome checks

runs:
  using: composite
  steps:
    - name: Setup Biome
      uses: biomejs/setup-biome@v2
    - name: Run Biome
      shell: bash
      run: biome ci .
